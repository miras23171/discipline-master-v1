import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.disciplinemaster.app',
  appName: 'Discipline Master',
  webDir: 'dist'
};

export default config;
