name: Android Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'zulu'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Accept Android Licenses
        run: yes | sdkmanager --licenses || true

      - name: Check Environment
        run: |
          node -v
          npm -v
          java -version

      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          npm list --depth=0

      - name: Lint and Type Check
        run: npm run lint

      - name: Build web app
        run: |
          npm run build
          ls -la dist

      - name: Capacitor Sync
        run: |
          npx cap sync android
          ls -R android/app/src/main/assets/public || echo "Assets not synced yet"

      - name: Check Android structure
        run: |
          ls -la android
          ls -la android/app

      - name: Build Android APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --info --stacktrace --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
